# convert the string into a list of characters
def sort_characters(string):
    character_list = list(string)
    
    #sort the list of characters alphabetically
    character_list = list(string)
    
    #convert the sorted list of characters back into a string
    stored_string =".join(character_list)+"
    
    #Return the sorted_string
    
    #Test the function with the given sample input
    string = "python"
    sorted_characters =sort_characters(string)
    print(f"string:/")
    print(f"sorted characters:/") 